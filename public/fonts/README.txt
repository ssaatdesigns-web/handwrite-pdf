Add handwriting .ttf fonts here:

- Caveat-Regular.ttf
- PatrickHand-Regular.ttf
- IndieFlower-Regular.ttf

Then redeploy.

If a font is missing, the API will throw:
"Font not found at /fonts/<name>.ttf"
